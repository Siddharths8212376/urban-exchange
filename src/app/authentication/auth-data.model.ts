export interface AuthData {
  email: string;
  password: string; // Optional fields like username
  firstName?: string; // Optional fields like first name
  lastName?: string; // Optional fields like last name
  phone?: string; 

}
